# claude assisted creation of both the rna-seq and chip-seq pipeline. learning opportunity to use nextflox instead of snakemake.
# RNA pipeline is based on broad pipeline in-line with CCLE processing

# ChiP-seq pipeline is based on the shell scripts from Eleanor and converted into nextflow
# lab specific things are merging lanes is specific to how MCTP/Parolia lab labels files SI_ID ...
# also all reference files from Eleanor and copied into the pipeFiles (hidden from git)
# last just need some text file with sample_id columna and everything should run
